server:
  port: 7001
eureka:
  instance:
    hostname: eurekaService7001.com
    prefer-ip-address: true
    ip-address: 127.0.0.1
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
#      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
      defaultZone: http://${eureka.instance.hostname}:${server.port}/eureka/
